const paginate = (props) => {
    console.error(props)
    switch (props.direction) {
        case 'next':
            return;
        case 'prev':
            return;
        default:
            return;
    }
}

export { paginate };